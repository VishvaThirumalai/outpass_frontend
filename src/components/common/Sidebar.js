// src/components/common/Sidebar.js - Empty component since we're using top navigation
import React from 'react';

const Sidebar = ({ menuItems }) => {
  return null; // Sidebar is no longer use
};

export default Sidebar;